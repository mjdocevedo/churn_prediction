FROM python:3.10-slim
RUN pip install mlflow==3.8.1
WORKDIR /mlflow
RUN mkdir -p /mlflow/data /mlflow/artifacts
EXPOSE 5001
CMD ["mlflow", "server", "--host", "0.0.0.0", "--port", "5001", "--backend-store-uri", "sqlite:////mlflow/data/mlflow.db", "--default-artifact-root", "/mlflow/artifacts"]